# Its own database. Never point this at the contabilidad-os DATABASE_URL:
# §1 of the outline only holds while this archive is single-user and isolated.
DATABASE_URL="postgresql://user:password@localhost:5432/memory_engine"

# Where the markdown export lands (npm run export).
MEMORY_EXPORT_DIR="./export"
